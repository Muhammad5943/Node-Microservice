apiVersion: v1
kind: Pod
metadata:
  name: posts
spec:
  containers:
    - name: posts
      image: muhammadajiputramp98/posts:0.0.1
      ports:
        - name: posts
          containerPort: 80
          protocol: TCP
      resources:
        limits:
          memory: 512Mi
          cpu: "1"
